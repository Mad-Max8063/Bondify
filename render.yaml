services:
  - type: web
    name: bondify
    env: node
    plan: free
    region: oregon
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: PORT
        value: 8001
      - key: VITE_BACKEND_URL
        value: ""
      - key: NODE_ENV
        value: production
